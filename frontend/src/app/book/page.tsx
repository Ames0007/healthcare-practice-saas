import { AreaPlaceholder } from "@/components/app/area-placeholder";

export default function BookPage() {
  return (
    <AreaPlaceholder titleKey="areas.book.title" descriptionKey="areas.book.description" />
  );
}
